> 归档时间：2026-09-18（CST）。阶段原始报告快照；当前状态及证据索引见上级 README，原本机绝对路径仅用于追溯。
>
> 原始来源：`Tracking/issues/test_correct_module_names_inductor_trace_handler/代码合入描述.md`。

# [Fix] Correct the public module of inductor_trace_handler

状态（2026-09-18 10:01 CST）：已 rebase 到 master `ec693356f`，
API 修复提交 / PR HEAD 现为 `195830924`；已推送并触发完整流水 #68280，运行中，未有最终结果。

# 【合入来源】

- [x] issue/工单：PR !46073 的公开 API 检查失败。

# 【修改方案】

在 `torch_npu.profiler` 导出层设置 `inductor_trace_handler.__module__ = __name__`，
保持公开导入、函数身份和签名不变。新增回归检查公开列表、对象同一性、模块名及 pickle
往返；没有修改完整公开 API 检查或增加 allowlist。

# 【资料变更】

本地文档仓新增 `docs/pr_46073_api_fix.md`，明确修复范围和验证边界。

# 【接口变更】

公开调用方式不变；仅将函数所属模块元数据纠正为 `torch_npu.profiler`。
不涉及 C/C++、后端参数兼容、trace 格式或模型语义变更。

# 【功能验证】

- 新增聚焦用例：FAIL → PASS。
- 完整 `test_correct_module_names`：仍 FAIL_EXISTING；失败项 47 → 46，
  唯一消除项为本 PR API，无新增失败。
- Python-only 隔离验证使用 2.14 环境，不替代远端 2.15 全量 CI。

# 【CheckList】

- [x] 变更仅涉及目标公开 API 与对应测试。
- [x] 未增加 CPU fallback、测试豁免或 skip。
- [x] 本地聚焦回归及 diff whitespace 检查通过。
- [ ] 完整原始公开 API 测试通过（仍有基线失败）。
- [ ] PR 新 HEAD 的远端流水通过（已推送并发送 compile 请求，尚未确认结果）。
