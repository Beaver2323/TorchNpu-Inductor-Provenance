> 归档时间：2026-09-18（CST）。阶段原始报告快照；当前状态及证据索引见上级 README，原本机绝对路径仅用于追溯。
>
> 原始来源：`Tracking/issues/test_correct_module_names_inductor_trace_handler/复现报告.md`。

# 复现报告

更新时间：2026-09-16 20:47（CST）。状态：FAIL，修复前已复现。

## 范围与环境

仅处理 PR !46073 新增公开入口 `torch_npu.profiler.inductor_trace_handler` 的元数据，
不处理 PyTorch 2.15 indexing 参数或旧 annotation 测试。

本地环境：aarch64 / Python 3.11 / PyTorch 2.14.0a0+git8e86e0a。
独立安装 `de350aa` wheel，不覆盖共享环境；其 profiler 初始化文件、处理器和公开 API
检查文件与当前 `7b8795bb9` 修复前版本一致。不是完整新 HEAD 或 CI 2.15 的回归。

## 原始失败

远端 #67808 的 `TestPublicBindings.test_correct_module_names` 报：
API 在 `torch_npu.profiler.__all__`，但 `__module__` 为私有模块
`torch_npu.profiler._inductor_profiler`。

本轮原始用例也复现该条目，exit code 1；另含其他现有 PyTorch 导出问题。
新增聚焦用例 `TestInductorProfiler.test_inductor_trace_handler_public_api`
也在相同模块名断言失败，exit code 1，无 skip。

## 复现命令与日志

所有测试从 `/home/z50063656/tmp` 启动。完整测试参数和导入版本、路径、文件哈希
均打印在以下日志中：

```text
/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/
  run_case.py
  before_metadata.log
  before_public.log
```

命令框架（分别以 `metadata` 和 `public` 为参数）：

```bash
cd /home/z50063656/tmp
source /usr/local/Ascend/cann9.0.1/cann-9.0.1/set_env.sh
export TORCH_DEVICE_BACKEND_AUTOLOAD=0 TORCHINDUCTOR_NPU_BACKEND=triton_experimental
export ASCEND_RT_VISIBLE_DEVICES=6
export PYTHONPATH=/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/runtime
export TORCH_COMPILE_DEBUG=1
export TORCHINDUCTOR_CACHE_DIR=/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/cache
/home/z50063656/envs/Tracking/bin/python \
  /home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/run_case.py public
```

`public` 执行未修改的原始 `test_correct_module_names`，没有过滤其失败条目。

## 禁用与测试语义

两项本地测试实际执行并 FAIL，没有被禁用或 skip。公开 API 检查与设备算力无关，
验证对象是 Python 函数导出规范；不需要编译模型。
