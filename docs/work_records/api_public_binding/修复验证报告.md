> 归档时间：2026-09-18（CST）。阶段原始报告快照；当前状态及证据索引见上级 README，原本机绝对路径仅用于追溯。
>
> 原始来源：`Tracking/issues/test_correct_module_names_inductor_trace_handler/修复验证报告.md`。

# 修复验证报告

更新时间：2026-09-18 10:01（CST，UTC+08:00）。

最新复验：已 rebase 到 master `ec693356f`，新 HEAD `195830924` 已推送并发送 `compile`
请求。聚焦回归再次 PASS；完整原用例仍 FAIL_EXISTING，46 项与上一轮完全相同，新增 0。
验证范围仍为下述隔离 Python 元数据检查，不是完整新 HEAD 或远端 2.15 的验收。
完整命令及日志见 `Tracking/triton_experimental_delivery/pr_rebase_20260918.xErqjW/README.md`。
最新远端确认：#68280 针对 `19583092` 运行中，尚无最终结果；不等于远端已通过。

结论：**本 PR 的目标元数据问题已修复，聚焦回归 PASS；完整公开 API 原用例仍为
FAIL_EXISTING，不能作为全量验收通过。已提交并推送到 PR !46073。**

## 问题描述

PR !46073 新增 `inductor_trace_handler` 到公开 `__all__`，但函数的 `__module__`
仍为私有实现模块，触发 `TestPublicBindings.test_correct_module_names`。
本轮授权只修该 PR 自身问题，不修其他基线/CI 问题。

## 修复内容摘要

- 修改文件：`torch_npu/profiler/__init__.py`、`test/profiler/test_inductor_profiler.py`。
- 修复方式：代码修复，显式声明公开函数的所属模块；新增防回归测试。
- 修复位置：`torch_npu.profiler` 包导出层。
- 是否包含 C/C++ 变更：否。
- 初次修复验证基于 `7b8795bb9`；用户授权推送后再次 rebase 到 master `0095e028c`，
  最终修复提交 / PR HEAD 为 `5efe8a254`，已推送。子模块原有修改未清理。

## 验证结果

### 原用例回归

| 用例 / 检查 | 修复前 | 修复后 | 说明 |
| --- | --- | --- | --- |
| `TestInductorProfiler.test_inductor_trace_handler_public_api` | FAIL，exit 1 | PASS，exit 0 | 同一个聚焦用例，新增断言实际先失败后通过 |
| `TestPublicBindings.test_correct_module_names` | FAIL，exit 1 | FAIL_EXISTING，exit 1 | 完整用例未修改、未过滤；问题项从 47 降至 46 |
| 完整失败集合对比 | 含目标 API | 目标 API 消失 | 仅删除 `torch_npu.profiler.inductor_trace_handler`；新增项为 0，其他 46 项不变 |
| 源码 / 隔离 runtime 文件一致性 | 修复前 hash 一致 | 修复后 hash 一致 | 确认测试没有误用共享旧包 |

其余 46 项位于 `torch.*`，属于本机既有源码环境的公开绑定失败；完整清单在
`result.json` 的 `unchanged` 字段。不修改这些项，不把它们过滤后称为原用例 PASS。

最终 rebase 后，推送前再次运行聚焦用例，PASS / exit 0；见 `pre_push_metadata.log`。
源码与隔离 runtime 的 profiler 初始化文件 SHA256 一致：
`1ec815a37c9d44ac3a6eb813a323fc3d6ebf13c746d79fc8ef538e6eb79d9295`。
没有重新构建整个新 HEAD，也没有把本地 2.14 聚焦复验称为 2.15 CI 通过。

### 近邻用例验证

未继续扩大测试：原完整用例仍有基线失败，按验证流程停止后续扩测；用户也限定只修
本 PR 问题。新增聚焦用例已检查公开导出、对象同一性、`__module__` 和 pickle 往返。

### 编译验证（如适用）

- 是否执行：否，仅 Python 元数据修复。
- 编译结果：未执行；复用上一轮已构建的 de350aa 原生 wheel 于独立 runtime。
- 环境边界：PyTorch `2.14.0a0+git8e86e0a`、Python 3.11、aarch64。该修复涉及的
  profiler 初始化、处理器及原始测试文件在 wheel / 当前 PR 修复前一致；修复后初始化
  文件也与当前工作树逐字节一致。不宣称完整 7b8795bb9 或 CI 2.15 已验证。

## 代码变更审查

修改文件清单同上。核心 diff：

```diff
 from ._inductor_profiler import inductor_trace_handler

+inductor_trace_handler.__module__ = __name__
+
 __all__ = [
```

改动只规范公开元数据，不改变函数签名、函数对象、handler 行为、trace 转换或
`indexing`。新增测试不是改松原有断言；没有改 allowlist / disabled 列表。
完整补丁保存为证据目录的 `fix.patch`。`git diff --check` 通过。

## 验证命令与关键输出

### 原用例回归命令

```bash
cd /home/z50063656/tmp
source /usr/local/Ascend/cann9.0.1/cann-9.0.1/set_env.sh
export TORCH_DEVICE_BACKEND_AUTOLOAD=0 TORCHINDUCTOR_NPU_BACKEND=triton_experimental
export ASCEND_RT_VISIBLE_DEVICES=6
export PYTHONPATH=/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/runtime
export TORCH_COMPILE_DEBUG=1
export TORCHINDUCTOR_CACHE_DIR=/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/cache
/home/z50063656/envs/Tracking/bin/python \
  /home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/run_case.py metadata
/home/z50063656/envs/Tracking/bin/python \
  /home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/run_case.py public
/home/z50063656/envs/Tracking/bin/python \
  /home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/compare_results.py
```

关键输出：

```text
聚焦测试：Ran 1 test / OK / exit 0
完整原用例：Ran 1 test / FAILED (failures=1) / exit 1
before_failure_items: 47
after_failure_items: 46
removed: ["torch_npu.profiler.inductor_trace_handler"]
added: []
```

完整原始输出、命令参数和导入路径在以下证据目录，未删减失败：

```text
/home/z50063656/Tracking/triton_experimental_delivery/pr_api_fix_20260916.ebZYKd/
  before_metadata.log
  before_public.log
  after_metadata.log
  after_public.log
  result.json
  fix.patch
```

### 近邻用例命令与输出

未执行，无对应 PASS 结论。没有进行模型编译或性能验证。

## 残余风险

- 全量原用例仍有 46 项修复前已存在的 PyTorch 公开 API 失败。
- 未验证 CI 2.15 ARM/Python 3.10 环境的整体流水。
- indexing 参数兼容、旧 annotation 测试错配按用户要求不修改，仍可能导致远端失败。
- 当前只修元数据，不代表新增 NPU 算子、dtype 或模型能力。
- 独立 wheel 未包含 torchair，导入会有其缺省 backend 提示；本轮不验证 torchair。
- 共享 PyTorch / torch_npu 安装没有修改，修复只在源码工作树及本轮独立测试副本。

### Handoff

- 阶段结论：聚焦修复 PASS；完整原用例 FAIL_EXISTING，未达到全量通过。
- 下一步动作：按用户范围保留本 PR 修复；不自行修改其他基线问题或扩大回归。
- 传递工件：本报告、代码合入描述、fix.patch、四份原始日志、result.json。
- 验证覆盖：无 C++ 重编译；目标回归先失败后通过；完整公开 API 失败集合对照；近邻未扩测。
- 残余风险等级：important（远端流水及其他基线失败尚未解决）。
- 阻塞条件：完整原用例有既有失败，不影响一行修复落地，但不能宣称 PR 可合入。

### 推送状态（21:54）

已用旧远端 SHA 为 lease 安全更新 PR 分支；远端 Git 与 PR API 均确认新 HEAD。
PR 为 open / mergeable=true，尚未确认新 HEAD 的流水启动或结果。
本次未代发 CI 命令，未推送 GitHub 文档仓；本报告仅本地更新。

### Handoff（2026-09-18 09:59，覆盖上方历史状态）

- 阶段结论：聚焦回归 PASS，完整原用例 FAIL_EXISTING；不扩测，不作全量通过判定。
- 下一步动作：按用户授权已 rebase / 推送 / 发送 compile，已确认 #68280 运行中，待最终结果。
- 传递工件：本报告、代码合入描述、本轮 README、两份原始测试日志、range_diff.log、push.log、trigger_ci.log。
- 验证覆盖：没有重新编译整个新 HEAD；聚焦用例 PASS；完整失败集合不变；近邻未扩测。
- 残余风险等级：important（远端 2.15 完整流水尚未确认）。
- 阻塞条件：本地既有公开 API 失败不因本次 rebase 消失；不把它们等同于远端失败。
