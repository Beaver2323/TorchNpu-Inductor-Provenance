# 2026-09-18 文档归档发布检查

更新时间：2026-09-18 10:38（CST，UTC+08:00）。

发布目标只允许为 `https://github.com/Beaver2323/TorchNpu-Inductor-Provenance.git` 的
`main` 分支；不是 GitCode 源码仓，也不是其他项目目录。

## 本次纳入与整理

- 保留并发布文档仓此前尚未提交的完整非测试 diff 讲解、schema 转换代码框、
  模块验证通俗总结和 9 月 16 日同次采集的静态 HTML / Perfetto trace 演示。
- 从 Tracking 精选归档 37 个文档 / 证据文件，包含主线程交接、范围约束、
  API 修复完整报告、原始日志、归因 JSON、rebase / 推送 / CI 触发 / issue 关联记录、CPU 演示。
- 另外 17 篇旧研究记录对应已有指南或历史摘要，合并保留结论，不重复维护并列页面。
- 更新 README、索引、交付状态及当前完整非测试补丁；原始演示不改日期或版本。
- 旧的不完整 `f030beadb → 4845c9289` 补丁由完整历史版和当前版替代，旧内容可从 Git 历史恢复。

## 已执行检查

| 检查 | 结果 / 边界 |
| --- | --- |
| Markdown 相对链接 | 检查代码框之外的相对链接，均指向本仓存在的文件或目录 |
| JSON | 27 份均可解析，原始版本、设备和测试状态保留 |
| Python 脚本 | 9 份 AST 解析通过；没有执行模型、导入 torch 或安装依赖 |
| 归档来源 | 37 项源 / 目标 SHA256 记录完整；22 份原始证据逐字节相同 |
| CPU JSON | 两份原文件无末尾换行，归档时仅补换行，解析后的数据相同 |
| 历史 / 当前完整补丁 | 两份文件均与固定 BASE→HEAD 的 `git diff --full-index --binary` 逐字节一致，排除 test/** |
| 当前补丁反向检查 | 在匹配源码 HEAD 执行 `git apply --reverse --check` 通过；未实际应用补丁 |
| 常见凭据模式 | 未发现 GitHub token、私钥、URL 内嵌账号密码、AWS key、token 查询值或 Bearer 值；检查过程中未读取凭据文件 |
| 体积 / 排除项 | 文档及演示合计约 2.3 MB，无大于 10 MiB 的单文件；未复制环境、wheel、缓存、认证脚本或完整源码树 |
| PR 状态快照 | 10:38 确认 HEAD 195830924、需求 #4909 已关联、CI #68280 运行中；不是最终通过 |

源文件映射和 SHA256 见[来源清单](./source_inventory.json)。检查脚本留在 Tracking 的
本轮记录目录，不依赖它才能阅读文档。

## 有意保留的格式与边界

- `pr_diff_walkthrough.md` 的五处仅含一个空格的行是原始 unified diff 上下文标记。
  `git diff --check` 会提示 trailing whitespace；没有删除这些字符来破坏完整 diff。
  下载的 `.patch` 文件同样保留原始 diff 格式，原始 `.txt` 日志保留测试输出。
- tlparse 自动生成 HTML 也包含行尾空格，作为原始可视化证据按原样保留，未为通过空白
  检查而重写 HTML。普通正文 / 脚本的空白检查排除这些原始产物和完整 diff 代码框。
- 普通正文与脚本另行检查；不能把上述原始格式告警谎称为整个仓库无条件通过空白检查。
- 本次只核对本地链接；未对所有历史外部网页进行存活检查。远端 PR / issue / CI 已单独查询。
- 敏感信息检查是常见模式扫描，不声称能识别所有任意格式秘密；未把凭据文件或认证脚本纳入提交。
- 阶段快照中的“未推送文档仓”描述当时的历史状态，当前阅读入口是[工作记录](./README.md)。
